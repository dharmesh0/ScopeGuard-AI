global:
  scrape_interval: 15s

scrape_configs:
  - job_name: scopeguard-api
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8000"]

*** Add File: infra/grafana/provisioning/datasources/datasource.yml
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true

